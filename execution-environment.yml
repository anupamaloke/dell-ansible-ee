---
version: 3

build_arg_defaults:
   ANSIBLE_GALAXY_CLI_COLLECTION_OPTS: '--ignore-certs'

dependencies:
   ansible_core:
      package_pip: ansible_core
   ansible_runner:
      package_pip: ansible-runner
   galaxy: requirements.yml
   python: requirements.txt
   system: bindep.txt
   ### Uncomment the below lines to build the EE with PyU4V 9.2.x
   ### By default, the latest PyU4V is always installed
   #exclude:
     #python:
       #- PyU4V>=10.0

images:
   base_image:
      name: docker.io/library/rockylinux:9.3

additional_build_steps:
   prepend_base:
      - RUN echo This is a prepend base command!

   prepend_final: |
      RUN whoami
      RUN cat /etc/os-release
   append_final:
      - RUN echo This is a post-install command!
      - RUN ls -la /etc
